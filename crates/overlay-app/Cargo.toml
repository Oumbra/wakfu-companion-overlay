[package]
name = "overlay-app"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
description = "Cablage minimal de L1 (overlay-ingest) pour l'observer tourner sur un vrai wakfu.log — pas encore l'overlay final, voir README.md."

[dependencies]
overlay-ingest = { path = "../overlay-ingest" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
